# Directory to clean old files from
clean_dir: "./temp"

# Delete files older than this many days
max_age_days: 7

# URL to download
download_url: "https://httpbin.org/robots.txt"

# Where to save the downloaded file
download_path: "./downloads/robots.txt"
